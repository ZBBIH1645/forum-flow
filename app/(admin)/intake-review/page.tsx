import { IntakeReview } from "@/components/intake-review";

export const metadata = {
  title: "Intake Review · Forum Placement Dashboard"
};

export default function IntakeReviewPage() {
  return <IntakeReview />;
}

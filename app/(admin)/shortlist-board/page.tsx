import { ShortlistBoard } from "@/components/shortlist-board";

export const metadata = {
  title: "Shortlist Board · Forum Placement Dashboard"
};

export default function ShortlistBoardPage() {
  return <ShortlistBoard />;
}
